## Fusiku Frontend (.env.example)
## Copy to `.env` and fill values for your environment.

# API base URL for the backend
VITE_API_URL=http://localhost:3001

# Optional: set to "true" to enable extra dev logging (if supported by the app)
VITE_DEBUG=false

