/** Central config re-exports — prefer `import { … } from '@/core/config'`. */
export { resolveApiV1BaseUrl, resolveBackendOrigin } from '../config/apiBase';
export { appConfig } from '../config/appConfig';
